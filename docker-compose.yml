services:
  redis:
    image: redis
    ports:
      - "6379:6379"

  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"
    volumes:
      - ./data/qdrant:/qdrant/storage

  backend:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis
      - qdrant